{
  "enable_memory": {
    "description": "启用长期记忆",
    "type": "bool",
    "hint": "注入记忆索引并向 LLM 提供 memory_read / memory_write / memory_search 工具。关闭后已存记忆保留在磁盘，仅停止注入与读写。",
    "default": true
  },
  "enable_global_memory": {
    "description": "启用全局记忆（跨会话共享的机器人自我记忆）",
    "type": "bool",
    "hint": "全局记忆存机器人自身的行为准则与偏好，写入后对所有群聊和私聊生效。默认关闭：机器人只读写当前会话的记忆，任何会话都影响不到它在其他会话的表现。开启前请想清楚：任何会话里的对话都可能促使机器人写入全局记忆，从而改变它在全部会话中的行为。关闭后已有的全局记忆文件保留在磁盘，仅停止注入与读写。",
    "default": false
  },
  "annotate_sender": {
    "description": "在用户消息前标注发送者身份",
    "type": "bool",
    "hint": "在每条用户消息开头注入 [发送者：…] 标注：QQ 群聊含 群名片｜QQ名｜QQ号，QQ 私聊含 QQ名｜QQ号，其他平台为 名称｜ID。LLM 以稳定且不可伪造的数字 ID 锚定记人——名称会变、可被冒用；正文中伪造的标注会被自动改写失效。关闭后群聊中无法可靠区分和记住不同的人。请同时保持 AstrBot 的「用户识别」为关闭，由本插件接管。",
    "default": true
  },
  "enable_compression": {
    "description": "启用上下文压缩",
    "type": "bool",
    "hint": "上下文占用超过阈值时自动滚动摘要旧轮次。平台聊天历史始终完整保留，压缩只改变发送给模型的视图。",
    "default": true
  },
  "max_context_tokens": {
    "description": "模型上下文窗口（tokens）",
    "type": "int",
    "hint": "强烈建议手动填写当前使用模型的上下文窗口大小（如 128000）。填 0 时按模型名自动探测；探测失败则压缩停用并在日志中提示。",
    "default": 0
  },
  "compress_threshold": {
    "description": "压缩触发阈值（占窗口比例）",
    "type": "float",
    "hint": "上下文估算占用超过 窗口×阈值 时触发滚动摘要。必须小于 0.82（AstrBot 内置压缩器的阈值），否则会被内置压缩器抢先；配置无效时自动钳制。默认 0.70。",
    "default": 0.7
  },
  "compress_buffer": {
    "description": "压缩目标缓冲区（占窗口比例）",
    "type": "float",
    "hint": "压缩完成后目标占用 = 阈值 - 缓冲区，预留空间避免刚压完又逼近阈值。默认 0.165，约留出窗口六分之一的余量。",
    "default": 0.165
  },
  "keep_recent_turns": {
    "description": "保留原文的最近轮数",
    "type": "int",
    "hint": "滚动摘要时最近 N 轮对话保留原文不进摘要（至少 1 轮）。默认 3。",
    "default": 3
  },
  "summary_provider_id": {
    "description": "摘要模型提供商",
    "type": "string",
    "hint": "指定负责生成压缩摘要的提供商；留空则使用当前对话的提供商。指定的提供商全部重试失败后会自动回退到当前对话提供商。",
    "default": "",
    "_special": "select_provider"
  },
  "summary_retry_count": {
    "description": "摘要重试次数",
    "type": "int",
    "hint": "摘要提供商调用失败时的最大尝试次数，默认 3。全链路失败后进入指数退避（5 分钟起、上限 1 小时），期间使用应急裁剪视图，不丢任何历史。",
    "default": 3
  },
  "summary_timeout": {
    "description": "摘要调用超时（秒）",
    "type": "int",
    "hint": "单次摘要调用的超时时间，默认 120 秒。",
    "default": 120
  },
  "summary_prompt_template": {
    "description": "自定义摘要提示词模板",
    "type": "text",
    "hint": "留空使用内置的结构化摘要模板。可用占位符：{previous_summary} 已有摘要、{transcript} 待压缩对话。模板损坏时自动回退内置模板。",
    "default": ""
  },
  "extract_memories": {
    "description": "压缩时联动提取记忆",
    "type": "bool",
    "hint": "滚动摘要的同一次 LLM 调用里，顺带把即将淡出上下文的长期事实提取为记忆文件（只写入会话作用域）。",
    "default": true
  }
}
